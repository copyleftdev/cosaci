[package]
name        = "cosaci-state"
version.workspace    = true
edition.workspace    = true
license.workspace    = true
repository.workspace = true
authors.workspace    = true
description = "CosaCI: stateful subsystems (leases, registries, partitions, sharding)"
readme      = "../../README.md"
keywords    = ["distributed", "ci", "lease", "sharding", "partition"]
categories  = ["data-structures", "concurrency"]
publish     = false

[dependencies]
cosaci-core = { path = "../cosaci-core" }
sha2        = { workspace = true }

[lints]
workspace = true
