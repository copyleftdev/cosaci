//! `cosaci-coordinator` binary.
//!
//! Stub. The working coordinator binary still lives at
//! `src/bin/coordinator.rs` (root cosaci package) until issue #4
//! moves it here.

fn main() {
    eprintln!(
        "cosaci-coordinator: not yet moved out of src/bin/. \
         Use `cargo run --bin coordinator` for the working v0.1 binary; \
         see issue #4 for the move PR."
    );
}
