//! `cosaci-agent` binary.
//!
//! Stub. The working agent binary still lives at `src/bin/agent.rs`
//! (root cosaci package) until issue #4 moves it here.

fn main() {
    eprintln!(
        "cosaci-agent: not yet moved out of src/bin/. \
         Use `cargo run --bin agent` for the working v0.1 binary; \
         see issue #4 for the move PR."
    );
}
