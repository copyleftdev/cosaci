[package]
name        = "cosaci-demo"
version.workspace    = true
edition.workspace    = true
license.workspace    = true
repository.workspace = true
authors.workspace    = true
description = "CosaCI single-process and networked demo runners — the smoke test for the full mesh"
readme      = "../../README.md"
keywords    = ["distributed", "ci", "demo", "smoke-test", "mtls"]
categories  = ["command-line-utilities", "development-tools::testing"]
publish     = false

[dependencies]
cosaci-core     = { path = "../../crates/cosaci-core" }
cosaci-state    = { path = "../../crates/cosaci-state" }
cosaci-protocol = { path = "../../crates/cosaci-protocol" }
cosaci-vrf      = { path = "../../crates/cosaci-vrf" }
cosaci-wasm     = { path = "../../crates/cosaci-wasm" }

[lints]
workspace = true
